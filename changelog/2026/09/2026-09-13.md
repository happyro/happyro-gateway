# 2026-09-13

- 修复 `?raw` 和源码重写路径的目录边界校验，拒绝仅共享根目录字符串前缀的兄弟路径；新增路径回归测试。

- WebSocket/TCP 转发改为双向流式背压，移除连接早期队列溢出时静默丢消息的路径。
- 限制单条 WebSocket 消息大小与 TCP 建连时间，统一两端关闭清理。
- 连接日志增加时间、关联 ID、目标、关闭原因及收发字节数。
- 新增真实 TCP/WebSocket 慢读、突发、完整性及失败清理测试。
